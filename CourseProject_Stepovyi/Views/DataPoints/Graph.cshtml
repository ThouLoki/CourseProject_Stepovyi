<h1>@ViewBag.random</h1>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <script src="https://canvasjs.com/assets/script/canvasjs.min.js"></script>
    @*<link rel="stylesheet" href="~/css/StyleSheet.css.css" />*@
    <title>Index</title>
</head>
<body>
    <br><br>
    <div id="chartContainer"></div>

    <script type="text/javascript">
        var JavaScriptArray = @Html.Raw(Json.Serialize(ViewBag.DataPoints));
        var result1 = @Html.Raw(Json.Serialize(ViewBag.LeastSquare));
        var result2= @Html.Raw(Json.Serialize(ViewBag.Module));
        window.onload = function () {
            var chart = new CanvasJS.Chart("chartContainer", {
                theme: "theme2",
				animationEnabled: true,
				title: {
                text: ""

                },
				subtitles: [

                    { text: "" }
				],
                data:
                [
                        {
                            color:"red",
                            type: "scatter", //change type to bar, line, area, pie, etc
                            dataPoints: JavaScriptArray /*JavaScriptArray*/
				    }
                    ,{
                        type: "line", //change type to bar, line, area, pie, etc
                        dataPoints: result1

                        },
                        {
                            color:"blue",
                            type: "line",
                            dataPoints: result2
                        }
				]
			});
        chart.render();
    };
    </script>

    <script>
        var textarea = document.getElementById("Kappa");
        var heightLimit = 200; /* Maximum height: 200px */
        textarea.oninput = function () {
            textarea.style.height = ""; /* Reset the height*/
            textarea.style.height = Math.min(textarea.scrollHeight, heightLimit) + "px";
        };
    </script>
    <script>
        var textarea = document.getElementById("Kappa");
        var heightLimit = 200; /* Maximum height: 200px */
        textarea.oninput = function () {
            textarea.style.height = ""; /* Reset the height*/
            textarea.style.height = Math.min(textarea.scrollHeight, heightLimit) + "px";
        };
    </script><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br /><br />
    <h4>OLS error = @ViewBag.Error</h4><br />
    <h4>k_least = @ViewBag.k_least</h4><br />
    <h4>b_least = @ViewBag.b_least</h4><br />
    <h4>Module error = @ViewBag.ModuleError</h4><br />
    <h4>k_module = @ViewBag.k_module</h4><br />
    <h4>b_module = @ViewBag.b_module</h4><br />

</body>
</html>
